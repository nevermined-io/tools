version: "3"

networks:
  backend:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.15.0.1/24

volumes:
  keeper-node-testing:
  keeper-node-staging:
  keeper-node-production:
  secret-store:
